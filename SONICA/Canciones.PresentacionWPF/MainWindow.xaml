<Window x:Class="Canciones.PresentacionWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Canciones.PresentacionWPF.ViewModels"
        mc:Ignorable="d"
        Title="Registro de Canciones Favoritas" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <GridSplitter Grid.Column="0" Width="5" HorizontalAlignment="Right" VerticalAlignment="Stretch" ResizeBehavior="PreviousAndNext"/>

        <!-- Panel izquierdo: Formulario -->
        <Border Grid.Column="0" Width="300" Margin="0,0,10,0" Padding="10" BorderBrush="Gray" BorderThickness="1" CornerRadius="8">
            <StackPanel>
                <TextBlock Text="DATOS DE LA CANCIÓN" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                <TextBlock Text="Título:"/>
                <TextBox Text="{Binding Titulo, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>
                <TextBlock Text="Artista:"/>
                <TextBox Text="{Binding Artista, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>
                <TextBlock Text="Género:"/>
                <TextBox Text="{Binding Genero, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>
                <TextBlock Text="Año :"/>
                <TextBox Text="{Binding Anio, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                <Button Content="Agregar" Command="{Binding AgregarCommand}" Margin="0,10,0,5" Padding="5" Background="LightGreen" />
                <Button Content="Actualizar" Command="{Binding ActualizarCommand}" Margin="0,5" Padding="5" Background="LightBlue"/>
                <Button Content="Limpiar" Command="{Binding LimpiarCommand}" Margin="0,5" Padding="5" Background="LightGray"/>
                <TextBlock Text="* Selecciona una canción para actualizar" FontSize="10" FontStyle="Italic" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <!-- Panel derecho: Listado y búsqueda -->
        <StackPanel Grid.Column="1">
            <GroupBox Header="Búsqueda" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBlock Text="Título:" VerticalAlignment="Center" Margin="5,0"/>
                    <TextBox Text="{Binding TituloBusqueda, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="5"/>
                    <TextBlock Text="Artista:" VerticalAlignment="Center" Margin="5,0"/>
                    <TextBox Text="{Binding ArtistaBusqueda, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="5"/>
                    <Button Content="Buscar" Command="{Binding BuscarCommand}" Margin="10,0" Padding="10,5"/>
                    <Button Content="Mostrar todos" Command="{Binding CargarCancionesCommand}" Padding="10,5"/>
                </StackPanel>
            </GroupBox>

            <DataGrid ItemsSource="{Binding Canciones}" SelectedItem="{Binding CancionSeleccionada, Mode=TwoWay}"
                      AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,10" Height="400">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" />
                    <DataGridTextColumn Header="Título" Binding="{Binding Titulo}" Width="*"/>
                    <DataGridTextColumn Header="Artista" Binding="{Binding Artista}" Width="*"/>
                    <DataGridTextColumn Header="Género" Binding="{Binding Genero}" Width="100"/>
                    <DataGridTextColumn Header="Año" Binding="{Binding Anio}" Width="70"/>
                </DataGrid.Columns>
            </DataGrid>

            <Button Content="Eliminar seleccionada" Command="{Binding EliminarCommand}" HorizontalAlignment="Right" Width="150" Padding="5" Background="LightCoral"/>
        </StackPanel>
    </Grid>
</Window>